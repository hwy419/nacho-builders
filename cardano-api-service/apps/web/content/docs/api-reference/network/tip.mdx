---
title: queryNetwork/tip
description: Query the network layer's current tip
category: api-reference
order: 3
ogmiosRef: https://ogmios.dev/api/#operation-publish-/?queryNetwork/tip
---

Query the current tip from the network layer. This returns the latest block known to the network, including additional metadata about the node's sync status.

## Request

```json
{
  "jsonrpc": "2.0",
  "method": "queryNetwork/tip"
}
```

No parameters required.

<APIPlayground method="queryNetwork/tip" />

## Response

```json
{
  "jsonrpc": "2.0",
  "method": "queryNetwork/tip",
  "result": {
    "slot": 123456789,
    "id": "8e8c93fb4a7c7e5a1a3e5f9b2c4d6e8f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d",
    "height": 10234567
  }
}
```

## Response Fields

| Field | Type | Description |
|-------|------|-------------|
| `slot` | `number` | Current slot number |
| `id` | `string` | Block hash (64 hex characters) |
| `height` | `number` | Block height (blocks since genesis) |

## Code Examples

<LanguageTabs examples={{
  javascript: `async function getNetworkTip() {
  const response = await fetch('https://api.nacho.builders/v1/ogmios', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'apikey': process.env.NACHO_API_KEY
    },
    body: JSON.stringify({
      jsonrpc: '2.0',
      method: 'queryNetwork/tip'
    })
  });

  const { result } = await response.json();
  return result;
}

const tip = await getNetworkTip();
console.log('Slot:', tip.slot);
console.log('Height:', tip.height);
console.log('Block ID:', tip.id);`,
  typescript: `interface NetworkTip {
  slot: number;
  id: string;
  height: number;
}

async function getNetworkTip(): Promise<NetworkTip> {
  const response = await fetch('https://api.nacho.builders/v1/ogmios', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'apikey': process.env.NACHO_API_KEY!
    },
    body: JSON.stringify({
      jsonrpc: '2.0',
      method: 'queryNetwork/tip'
    })
  });

  const { result } = await response.json();
  return result;
}

// Calculate slot utilization
async function getSlotUtilization(): Promise<number> {
  const tip = await getNetworkTip();
  return tip.height / tip.slot;
}`,
  python: `import requests
import os
from datetime import datetime

def get_network_tip() -> dict:
    response = requests.post(
        'https://api.nacho.builders/v1/ogmios',
        headers={
            'Content-Type': 'application/json',
            'apikey': os.environ['NACHO_API_KEY']
        },
        json={
            'jsonrpc': '2.0',
            'method': 'queryNetwork/tip'
        }
    )
    return response.json()['result']

tip = get_network_tip()

# Convert slot to approximate time
SHELLEY_START_SLOT = 4492800
SHELLEY_START = datetime(2020, 7, 29, 21, 44, 51)

slots_since_shelley = tip['slot'] - SHELLEY_START_SLOT
from datetime import timedelta
block_time = SHELLEY_START + timedelta(seconds=slots_since_shelley)

print(f"Current slot: {tip['slot']:,}")
print(f"Block height: {tip['height']:,}")
print(f"Approximate time: {block_time.isoformat()}")`,
  go: `type NetworkTip struct {
    Slot   int64  \`json:"slot"\`
    ID     string \`json:"id"\`
    Height int64  \`json:"height"\`
}

func getNetworkTip() (*NetworkTip, error) {
    payload := map[string]interface{}{
        "jsonrpc": "2.0",
        "method":  "queryNetwork/tip",
    }

    body, _ := json.Marshal(payload)

    req, _ := http.NewRequest("POST", "https://api.nacho.builders/v1/ogmios", bytes.NewBuffer(body))
    req.Header.Set("Content-Type", "application/json")
    req.Header.Set("apikey", os.Getenv("NACHO_API_KEY"))

    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        return nil, err
    }
    defer resp.Body.Close()

    var result struct {
        Result NetworkTip \`json:"result"\`
    }
    json.NewDecoder(resp.Body).Decode(&result)

    return &result.Result, nil
}`,
  rust: `#[derive(Deserialize)]
struct NetworkTip {
    slot: u64,
    id: String,
    height: u64,
}

async fn get_network_tip(client: &Client) -> Result<NetworkTip, Error> {
    let payload = json!({
        "jsonrpc": "2.0",
        "method": "queryNetwork/tip"
    });

    let response: serde_json::Value = client
        .post("https://api.nacho.builders/v1/ogmios")
        .header("Content-Type", "application/json")
        .header("apikey", std::env::var("NACHO_API_KEY")?)
        .json(&payload)
        .send()
        .await?
        .json()
        .await?;

    Ok(serde_json::from_value(response["result"].clone())?)
}`,
  curl: `curl -X POST https://api.nacho.builders/v1/ogmios \\
  -H "Content-Type: application/json" \\
  -H "apikey: $NACHO_API_KEY" \\
  -d '{"jsonrpc": "2.0", "method": "queryNetwork/tip"}'`
}} />

## Comparing Tip Queries

| Query | Returns | Use Case |
|-------|---------|----------|
| `queryNetwork/tip` | Network layer tip with height | General purpose, includes block height |
| `queryLedgerState/tip` | Ledger state tip | Ledger queries, state calculations |

The network tip may be slightly ahead of the ledger tip during block processing.

## Sync Status Check

```javascript
async function checkSyncStatus() {
  const tip = await getNetworkTip();

  // Calculate expected slot based on current time
  const SHELLEY_START_SLOT = 4492800;
  const SHELLEY_START_TIME = new Date('2020-07-29T21:44:51Z').getTime();
  const now = Date.now();

  const expectedSlot = SHELLEY_START_SLOT + Math.floor((now - SHELLEY_START_TIME) / 1000);
  const slotDiff = expectedSlot - tip.slot;

  if (slotDiff < 100) {
    console.log('Node is fully synced');
  } else if (slotDiff < 7200) {
    console.log(`Node is ${slotDiff} slots behind (~${Math.round(slotDiff / 60)} minutes)`);
  } else {
    console.log(`Node is syncing: ${slotDiff} slots behind`);
  }

  return {
    currentSlot: tip.slot,
    expectedSlot,
    slotsBehind: slotDiff,
    height: tip.height
  };
}
```

<Callout type="tip">
  For chain synchronization and real-time block monitoring, use WebSocket connections with `findIntersection` and `nextBlock` instead of polling the tip.
</Callout>

## Use Cases

- **Health monitoring** - Check if node is synced
- **Dashboard displays** - Show current blockchain stats
- **TTL calculation** - Determine transaction expiry slots
- **Epoch tracking** - Calculate current epoch from slot

<ExternalLink href="https://ogmios.dev/api/#operation-publish-/?queryNetwork/tip">
  Full Ogmios API Reference
</ExternalLink>
